package com.seadrip.enchanthelper.items;

import net.minecraft.world.food.FoodProperties;
import net.minecraft.world.item.Item;
import net.minecraftforge.registries.DeferredRegister;
import net.minecraftforge.registries.RegistryObject;

public class EhItems {
    // Creates a new food item with the id "enchanthelper:example_id", nutrition 1 and saturation 2
    // public final RegistryObject<Item> EXAMPLE_ITEM;

    public EhItems(DeferredRegister<Item> ir) {
        // EXAMPLE_ITEM = ir.register("example_item", () -> new Item(new Item.Properties().food(new FoodProperties.Builder()
        //         .alwaysEat().nutrition(1).saturationMod(2f).build())));
    }
}
